[build]
  command = "npm run build"
  publish = "./"

[build.environment]
  NODE_VERSION = "18"
  SECRETS_SCAN_OMIT_PATHS = "config.js"

# Process environment variables for the build
[build.processing]
  skip_processing = true
